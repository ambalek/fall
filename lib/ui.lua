-- luacheck: globals include
local Menu = include("lib/ui.menu")
local Page = include("lib/ui.page")
local Text = include("lib/ui.text")
local UI = {
  Menu = Menu,
  Page = Page,
  Text = Text
}
return UI